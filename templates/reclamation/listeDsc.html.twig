{% extends 'backend/backend.html.twig' %}

{% block title %}Reclamation index{% endblock %}

{% block content %}
    <div class="card-header">
        <a class="btn btn-block btn-success" href="{{ path('list_reclamationfrontASC') }}" >Order By nomr  asc </a>
        <a class="btn btn-block btn-success" href="{{ path('list_reclamationfrontDESC') }}" >Order By nomr Desc </a>
    </div>
    <h1>Reclamation index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Idreclamation</th>
            <th>Nomr</th>
            <th>Sujetr</th>
            <th>Dater</th>
            <th>User</th>

        </tr>
        </thead>
        <tbody>
        {% for reclamation in reclamation %}
            <tr>
                <td>{{ reclamation.idreclamation }}</td>
                <td>{{ reclamation.nomr }}</td>
                <td>{{ reclamation.sujetr }}</td>
                <td>{{ reclamation.dater }}</td>
                <td>{{ reclamation.user }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('reclamation_new') }}">Create new</a>
{% endblock %}
